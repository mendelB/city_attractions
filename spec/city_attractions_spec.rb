require "spec_helper"

describe CityAttractions do
  it "has a version number" do
    expect(CityAttractions::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
